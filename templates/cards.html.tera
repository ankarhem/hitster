<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hitster Cards - {{ title }}</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        /* Print-specific customizations that work with Tailwind */
        @page {
            size: 210mm 297mm;
            margin: 0;
        }
        
        /* Exact card dimensions for A4 paper - 3x4 grid of 70mm squares */
        .card {
            width: 70mm;
            height: 70mm;
            page-break-inside: avoid;
            break-inside: avoid;
        }
        
        .qr-code {
            width: 22mm;
            height: 22mm;
        }
        
        /* Text truncation for long titles/artists */
        .line-clamp-2 {
            display: -webkit-box;
            -webkit-line-clamp: 2;
            -webkit-box-orient: vertical;
            overflow: hidden;
        }
        
        .line-clamp-3 {
            display: -webkit-box;
            -webkit-line-clamp: 3;
            -webkit-box-orient: vertical;
            overflow: hidden;
        }
        
        /* Print media - exact A4 layout with no margins */
        @media print {
            body {
                margin: 0;
                padding: 0;
                background: white;
                width: 210mm;
                height: 297mm;
                overflow: hidden;
            }
            
            .card-grid {
                display: grid !important;
                grid-template-columns: repeat(3, 70mm) !important;
                grid-template-rows: repeat(4, 70mm) !important;
                gap: 0 !important;
                width: 210mm !important;
                height: 280mm !important;
                margin: 0 !important;
                padding: 0 !important;
            }
            
            .card {
                border: 1px solid #000 !important;
                margin: 0 !important;
                padding: 0 !important;
                page-break-inside: avoid !important;
                break-inside: avoid !important;
            }
            
            .qr-code {
                width: 22mm !important;
                height: 22mm !important;
            }
            
            /* Hide any view-specific elements */
            .view-only {
                display: none !important;
            }
        }
        
        /* View mode - centered with padding for better UX */
        @media screen {
            .card-grid {
                display: grid;
                grid-template-columns: repeat(3, 70mm);
                grid-template-rows: repeat(4, 70mm);
                gap: 8px;
                justify-content: center;
                padding: 20px;
                background: #f5f5f5;
                min-height: 100vh;
            }
            
            .card {
                border: 2px solid #333;
                background: white;
                box-shadow: 0 2px 8px rgba(0,0,0,0.1);
                transition: transform 0.2s ease;
            }
            
            .card:hover {
                transform: scale(1.02);
            }
        }
    </style>
</head>
<body class="bg-gray-100">
    <!-- View mode info (hidden in print) -->
    <div class="view-only bg-blue-50 border-b border-blue-200 p-4 text-center">
        <h2 class="text-lg font-semibold text-blue-800 mb-1">Hitster Cards - {{ title }}</h2>
        <p class="text-sm text-blue-600">{{ cards|length }} songs • Print or save as PDF for perfect 70×70mm cards</p>
        <p class="text-xs text-blue-500 mt-1">A4 paper • 3×4 grid • No cutting margins needed</p>
    </div>
    
    <!-- Cards Grid -->
    <div class="card-grid">
        {% for card in cards %}
        <div class="card bg-white flex flex-col p-2">
            <!-- Song Info -->
            <div class="flex-1 flex flex-col">
                <h3 class="text-[10pt] font-bold leading-tight line-clamp-2 mb-1" title="{{ card.title }}">
                    {{ card.title }}
                </h3>
                <p class="text-[8pt] text-gray-600 leading-tight line-clamp-3 mb-2" title="{{ card.artist }}">
                    {{ card.artist }}
                </p>
                
                <div class="flex justify-between items-center mt-auto">
                    <span class="text-[7pt] bg-gray-100 px-1.5 py-0.5 rounded text-gray-700">
                        {{ card.year }}
                    </span>
                    <span class="text-[7pt] text-green-600 font-medium">
                        ♫ Spotify
                    </span>
                </div>
            </div>
            
            <!-- QR Code Section -->
            <div class="flex items-center justify-center pt-1 mt-1 border-t border-gray-200">
                {% if card.qr_data_url %}
                <img src="{{ card.qr_data_url }}" alt="QR Code" class="qr-code">
                {% else %}
                <div class="text-center text-gray-400">
                    <div class="text-lg mb-0.5">♪</div>
                    <div class="text-[5pt]">QR unavailable</div>
                </div>
                {% endif %}
            </div>
        </div>
        {% endfor %}
    </div>
</body>
</html>