<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hitster Cards - {{ title }}</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        /* Print-specific customizations that work with Tailwind */
        @page {
            size: A4;
            margin: 8mm;
        }
        
        /* Custom card dimensions and print optimizations */
        .card {
            width: 90mm;
            height: 55mm;
            page-break-inside: avoid;
            break-inside: avoid;
        }
        
        .qr-code {
            width: 18mm;
            height: 18mm;
        }
        
        /* Text truncation for long titles/artists */
        .line-clamp-2 {
            display: -webkit-box;
            -webkit-line-clamp: 2;
            -webkit-box-orient: vertical;
            overflow: hidden;
        }
        
        /* Print media adjustments */
        @media print {
            body {
                margin: 0;
                padding: 0;
                background: white;
            }
            
            /* Remove any interactive elements in print */
            .no-print {
                display: none !important;
            }
        }
    </style>
</head>
<body class="bg-white text-black font-sans leading-normal">
    <!-- Page Header -->
    <div class="text-center mb-8 break-after-avoid">
        <h1 class="text-2xl font-bold mb-2">Hitster Cards</h1>
        <p class="text-sm text-gray-600">{{ title }} • {{ cards|length }} songs</p>
    </div>
    
    <!-- Cards Grid -->
    <div class="grid grid-cols-2 gap-[5mm] max-w-none">
        {% for card in cards %}
        <div class="card border border-black bg-white p-[4mm] flex flex-col">
            <!-- Card Content -->
            <div class="flex-1 flex flex-col justify-between mb-[3mm]">
                <div>
                    <h3 class="text-xs font-bold mb-[2mm] leading-tight line-clamp-2" title="{{ card.title }}">
                        {{ card.title }}
                    </h3>
                    <p class="text-[9pt] mb-[1mm] leading-tight line-clamp-2" title="{{ card.artist }}">
                        {{ card.artist }}
                    </p>
                </div>
                
                <div class="flex justify-between items-center mt-auto">
                    <span class="text-[8pt] bg-gray-100 px-[2mm] py-[1mm] rounded-full">
                        {{ card.year }}
                    </span>
                    <span class="text-[8pt] text-green-600 font-medium">
                        ♫ Spotify
                    </span>
                </div>
            </div>
            
            <!-- QR Code Section -->
            <div class="border-t border-gray-200 pt-[3mm] flex items-center justify-center h-[20mm]">
                {% if card.qr_data_url %}
                <img src="{{ card.qr_data_url }}" alt="QR Code" class="qr-code">
                {% else %}
                <div class="text-center text-gray-400">
                    <div class="text-2xl mb-1">♪</div>
                    <div class="text-[6pt]">QR unavailable</div>
                </div>
                {% endif %}
            </div>
            
            <!-- Card Footer -->
            <div class="text-center text-[6pt] text-gray-500 mt-[2mm]">
                Scan to play on Spotify
            </div>
        </div>
        {% endfor %}
    </div>
</body>
</html>